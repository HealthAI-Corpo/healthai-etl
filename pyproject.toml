[project]
name = "etl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ruff>=0.15.6",
    "pytest>=8.0.0",
    "pandas>=2.0.0",
    "sqlalchemy>=2.0.0",
]

[tool.ruff]
exclude = ["notebooks/"]
